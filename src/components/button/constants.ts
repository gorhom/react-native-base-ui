enum KIND {
  primary = 0,
  secondary,
  tertiary,
  minimal,
}

enum SHAPE {
  default = 0,
  pill,
  round,
  circle,
  square,
}

enum SIZE {
  default = 0,
  mini,
  compact,
  large,
}

export { KIND, SHAPE, SIZE };
