enum INPUT_SIZE {
  default = 'default',
  large = 'large',
  compact = 'compact',
  mini = 'mini',
}

enum INPUT_ADJOINED {
  none = 'none',
  start = 'start',
  end = 'end',
  both = 'both',
}

const DEFAULT_CLEARABLE = false;

export { INPUT_SIZE, INPUT_ADJOINED, DEFAULT_CLEARABLE };
