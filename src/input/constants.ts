enum SIZE {
  default = 'default',
  large = 'large',
  compact = 'compact',
  mini = 'mini',
}

enum ADJOINED {
  none = 'none',
  left = 'left',
  right = 'right',
  both = 'both',
}

export { SIZE, ADJOINED };
