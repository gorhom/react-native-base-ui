enum BUTTON_KIND {
  primary = 'primary',
  secondary = 'secondary',
  tertiary = 'tertiary',
  minimal = 'minimal',
}

enum BUTTON_SHAPE {
  default = 'default',
  pill = 'pill',
  // @todo, maybe delete this one
  round = 'round',
  circle = 'circle',
  square = 'square',
}

enum BUTTON_SIZE {
  default = 'default',
  large = 'large',
  compact = 'compact',
  mini = 'mini',
}

export { BUTTON_KIND, BUTTON_SHAPE, BUTTON_SIZE };
