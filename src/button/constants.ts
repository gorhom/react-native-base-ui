enum KIND {
  primary = 'primary',
  secondary = 'secondary',
  tertiary = 'tertiary',
  minimal = 'minimal',
}

enum SHAPE {
  default = 'default',
  pill = 'pill',
  // @todo, maybe delete this one
  round = 'round',
  circle = 'circle',
  square = 'square',
}

enum SIZE {
  default = 'default',
  large = 'large',
  compact = 'compact',
  mini = 'mini',
}

export { KIND, SHAPE, SIZE };
